---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: clusteroptimizationconfigs.futura.io.lumos
spec:
  group: futura.io.lumos
  names:
    kind: ClusterOptimizationConfig
    listKind: ClusterOptimizationConfigList
    plural: clusteroptimizationconfigs
    singular: clusteroptimizationconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ClusterOptimizationConfig is the Schema for the clusteroptimizationconfigs
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterOptimizationConfigSpec defines the desired state of
              ClusterOptimizationConfig
            properties:
              apiKey:
                description: ApiKey specifies which customer/cluster this optimization
                  is for.
                type: string
              clusterScalingMode:
                default: recommend
                description: |-
                  ClusterScalingMode controls whether the operator automatically scales the cluster or only provides recommendations.
                  Valid values: "auto" (automatic scaling), "recommend" (recommendations only).
                  Defaults to "recommend" if not specified.
                enum:
                - auto
                - recommend
                type: string
              costOptimization:
                description: CostOptimization settings for budget and instance preferences.
                properties:
                  costSensitivity:
                    type: string
                  maxMonthlyBudgetUSD:
                    type: string
                  maxSpotPercentage:
                    type: string
                  preferredInstanceTypes:
                    items:
                      type: string
                    type: array
                  spotInstanceAllowed:
                    type: boolean
                type: object
              quotas:
                additionalProperties:
                  type: integer
                description: Quotas defines resource limits such as CPU, memory, etc.
                type: object
              scalingPolicies:
                description: ScalingPolicies defines which scaling mechanisms to enable.
                properties:
                  enableHPA:
                    type: boolean
                  enableNodeHandler:
                    type: boolean
                  enableVPA:
                    type: boolean
                required:
                - enableHPA
                - enableNodeHandler
                - enableVPA
                type: object
              syncPeriodSeconds:
                description: SyncPeriodSeconds defines how often the optimization
                  loop runs.
                minimum: 10
                type: integer
            required:
            - apiKey
            - scalingPolicies
            type: object
          status:
            description: ClusterOptimizationConfigStatus defines the observed state
              of ClusterOptimizationConfig
            properties:
              lastSynced:
                format: date-time
                type: string
              synced:
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
