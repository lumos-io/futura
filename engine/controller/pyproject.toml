[project]
name = "rl-controller"
version = "0.1.0"
description = "This is the Reinforcement Learning (RL) model that will determine the appropria usage
for each Deployment in a given cluster"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kubernetes>=33.1.0",
    "matplotlib>=3.10.6",
    "numpy>=2.3.2",
    "pandas>=2.3.2",
    "psutil>=7.0.0",
    "requests>=2.32.5",
    "torch>=2.8.0",
]
