// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v6.31.1
// source: user.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "backend";

export enum UserStatus {
  UNDEFINED_USER_STATUS = "UNDEFINED_USER_STATUS",
  USER_ACTIVE = "USER_ACTIVE",
  USER_INVITED = "USER_INVITED",
  USER_INACTIVE = "USER_INACTIVE",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function userStatusFromJSON(object: any): UserStatus {
  switch (object) {
    case 0:
    case "UNDEFINED_USER_STATUS":
      return UserStatus.UNDEFINED_USER_STATUS;
    case 1:
    case "USER_ACTIVE":
      return UserStatus.USER_ACTIVE;
    case 2:
    case "USER_INVITED":
      return UserStatus.USER_INVITED;
    case 3:
    case "USER_INACTIVE":
      return UserStatus.USER_INACTIVE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UserStatus.UNRECOGNIZED;
  }
}

export function userStatusToJSON(object: UserStatus): string {
  switch (object) {
    case UserStatus.UNDEFINED_USER_STATUS:
      return "UNDEFINED_USER_STATUS";
    case UserStatus.USER_ACTIVE:
      return "USER_ACTIVE";
    case UserStatus.USER_INVITED:
      return "USER_INVITED";
    case UserStatus.USER_INACTIVE:
      return "USER_INACTIVE";
    case UserStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function userStatusToNumber(object: UserStatus): number {
  switch (object) {
    case UserStatus.UNDEFINED_USER_STATUS:
      return 0;
    case UserStatus.USER_ACTIVE:
      return 1;
    case UserStatus.USER_INVITED:
      return 2;
    case UserStatus.USER_INACTIVE:
      return 3;
    case UserStatus.UNRECOGNIZED:
    default:
      return -1;
  }
}

export enum UserRole {
  UNDEFINED_USER_ROLE = "UNDEFINED_USER_ROLE",
  USER_ADMIN = "USER_ADMIN",
  USER_MANAGER = "USER_MANAGER",
  USER_DEVELOPER = "USER_DEVELOPER",
  USER_VIEWER = "USER_VIEWER",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function userRoleFromJSON(object: any): UserRole {
  switch (object) {
    case 0:
    case "UNDEFINED_USER_ROLE":
      return UserRole.UNDEFINED_USER_ROLE;
    case 1:
    case "USER_ADMIN":
      return UserRole.USER_ADMIN;
    case 2:
    case "USER_MANAGER":
      return UserRole.USER_MANAGER;
    case 3:
    case "USER_DEVELOPER":
      return UserRole.USER_DEVELOPER;
    case 4:
    case "USER_VIEWER":
      return UserRole.USER_VIEWER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UserRole.UNRECOGNIZED;
  }
}

export function userRoleToJSON(object: UserRole): string {
  switch (object) {
    case UserRole.UNDEFINED_USER_ROLE:
      return "UNDEFINED_USER_ROLE";
    case UserRole.USER_ADMIN:
      return "USER_ADMIN";
    case UserRole.USER_MANAGER:
      return "USER_MANAGER";
    case UserRole.USER_DEVELOPER:
      return "USER_DEVELOPER";
    case UserRole.USER_VIEWER:
      return "USER_VIEWER";
    case UserRole.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function userRoleToNumber(object: UserRole): number {
  switch (object) {
    case UserRole.UNDEFINED_USER_ROLE:
      return 0;
    case UserRole.USER_ADMIN:
      return 1;
    case UserRole.USER_MANAGER:
      return 2;
    case UserRole.USER_DEVELOPER:
      return 3;
    case UserRole.USER_VIEWER:
      return 4;
    case UserRole.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface User {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  role: UserRole;
  teams: string[];
  created_at: string;
  last_access: string;
  status: UserStatus;
  avatar: string;
  provider: string;
}

export interface GetUsersResponse {
  users: User[];
}

export interface InviteUserRequest {
  first_name: string;
  last_name: string;
  email: string;
  role: UserRole;
  team_ids: number[];
}

export interface UpdateUserRequest {
  first_name: string;
  last_name: string;
  email: string;
  role: UserRole;
  team_ids: number[];
  status: UserStatus;
}

function createBaseUser(): User {
  return {
    id: 0,
    first_name: "",
    last_name: "",
    email: "",
    role: UserRole.UNDEFINED_USER_ROLE,
    teams: [],
    created_at: "",
    last_access: "",
    status: UserStatus.UNDEFINED_USER_STATUS,
    avatar: "",
    provider: "",
  };
}

export const User: MessageFns<User> = {
  encode(message: User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int64(message.id);
    }
    if (message.first_name !== "") {
      writer.uint32(18).string(message.first_name);
    }
    if (message.last_name !== "") {
      writer.uint32(26).string(message.last_name);
    }
    if (message.email !== "") {
      writer.uint32(34).string(message.email);
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      writer.uint32(40).int32(userRoleToNumber(message.role));
    }
    for (const v of message.teams) {
      writer.uint32(50).string(v!);
    }
    if (message.created_at !== "") {
      writer.uint32(58).string(message.created_at);
    }
    if (message.last_access !== "") {
      writer.uint32(66).string(message.last_access);
    }
    if (message.status !== UserStatus.UNDEFINED_USER_STATUS) {
      writer.uint32(72).int32(userStatusToNumber(message.status));
    }
    if (message.avatar !== "") {
      writer.uint32(82).string(message.avatar);
    }
    if (message.provider !== "") {
      writer.uint32(90).string(message.provider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.first_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.last_name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.role = userRoleFromJSON(reader.int32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.teams.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.created_at = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.last_access = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.status = userStatusFromJSON(reader.int32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.avatar = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.provider = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): User {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      first_name: isSet(object.first_name) ? globalThis.String(object.first_name) : "",
      last_name: isSet(object.last_name) ? globalThis.String(object.last_name) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      role: isSet(object.role) ? userRoleFromJSON(object.role) : UserRole.UNDEFINED_USER_ROLE,
      teams: globalThis.Array.isArray(object?.teams) ? object.teams.map((e: any) => globalThis.String(e)) : [],
      created_at: isSet(object.created_at) ? globalThis.String(object.created_at) : "",
      last_access: isSet(object.last_access) ? globalThis.String(object.last_access) : "",
      status: isSet(object.status) ? userStatusFromJSON(object.status) : UserStatus.UNDEFINED_USER_STATUS,
      avatar: isSet(object.avatar) ? globalThis.String(object.avatar) : "",
      provider: isSet(object.provider) ? globalThis.String(object.provider) : "",
    };
  },

  toJSON(message: User): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.first_name !== "") {
      obj.first_name = message.first_name;
    }
    if (message.last_name !== "") {
      obj.last_name = message.last_name;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      obj.role = userRoleToJSON(message.role);
    }
    if (message.teams?.length) {
      obj.teams = message.teams;
    }
    if (message.created_at !== "") {
      obj.created_at = message.created_at;
    }
    if (message.last_access !== "") {
      obj.last_access = message.last_access;
    }
    if (message.status !== UserStatus.UNDEFINED_USER_STATUS) {
      obj.status = userStatusToJSON(message.status);
    }
    if (message.avatar !== "") {
      obj.avatar = message.avatar;
    }
    if (message.provider !== "") {
      obj.provider = message.provider;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<User>, I>>(base?: I): User {
    return User.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<User>, I>>(object: I): User {
    const message = createBaseUser();
    message.id = object.id ?? 0;
    message.first_name = object.first_name ?? "";
    message.last_name = object.last_name ?? "";
    message.email = object.email ?? "";
    message.role = object.role ?? UserRole.UNDEFINED_USER_ROLE;
    message.teams = object.teams?.map((e) => e) || [];
    message.created_at = object.created_at ?? "";
    message.last_access = object.last_access ?? "";
    message.status = object.status ?? UserStatus.UNDEFINED_USER_STATUS;
    message.avatar = object.avatar ?? "";
    message.provider = object.provider ?? "";
    return message;
  },
};

function createBaseGetUsersResponse(): GetUsersResponse {
  return { users: [] };
}

export const GetUsersResponse: MessageFns<GetUsersResponse> = {
  encode(message: GetUsersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.users) {
      User.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.users.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUsersResponse {
    return { users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => User.fromJSON(e)) : [] };
  },

  toJSON(message: GetUsersResponse): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUsersResponse>, I>>(base?: I): GetUsersResponse {
    return GetUsersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUsersResponse>, I>>(object: I): GetUsersResponse {
    const message = createBaseGetUsersResponse();
    message.users = object.users?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

function createBaseInviteUserRequest(): InviteUserRequest {
  return { first_name: "", last_name: "", email: "", role: UserRole.UNDEFINED_USER_ROLE, team_ids: [] };
}

export const InviteUserRequest: MessageFns<InviteUserRequest> = {
  encode(message: InviteUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.first_name !== "") {
      writer.uint32(10).string(message.first_name);
    }
    if (message.last_name !== "") {
      writer.uint32(18).string(message.last_name);
    }
    if (message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      writer.uint32(32).int32(userRoleToNumber(message.role));
    }
    writer.uint32(42).fork();
    for (const v of message.team_ids) {
      writer.int64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InviteUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInviteUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.first_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.last_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.role = userRoleFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.team_ids.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.team_ids.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InviteUserRequest {
    return {
      first_name: isSet(object.first_name) ? globalThis.String(object.first_name) : "",
      last_name: isSet(object.last_name) ? globalThis.String(object.last_name) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      role: isSet(object.role) ? userRoleFromJSON(object.role) : UserRole.UNDEFINED_USER_ROLE,
      team_ids: globalThis.Array.isArray(object?.team_ids) ? object.team_ids.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: InviteUserRequest): unknown {
    const obj: any = {};
    if (message.first_name !== "") {
      obj.first_name = message.first_name;
    }
    if (message.last_name !== "") {
      obj.last_name = message.last_name;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      obj.role = userRoleToJSON(message.role);
    }
    if (message.team_ids?.length) {
      obj.team_ids = message.team_ids.map((e) => Math.round(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InviteUserRequest>, I>>(base?: I): InviteUserRequest {
    return InviteUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InviteUserRequest>, I>>(object: I): InviteUserRequest {
    const message = createBaseInviteUserRequest();
    message.first_name = object.first_name ?? "";
    message.last_name = object.last_name ?? "";
    message.email = object.email ?? "";
    message.role = object.role ?? UserRole.UNDEFINED_USER_ROLE;
    message.team_ids = object.team_ids?.map((e) => e) || [];
    return message;
  },
};

function createBaseUpdateUserRequest(): UpdateUserRequest {
  return {
    first_name: "",
    last_name: "",
    email: "",
    role: UserRole.UNDEFINED_USER_ROLE,
    team_ids: [],
    status: UserStatus.UNDEFINED_USER_STATUS,
  };
}

export const UpdateUserRequest: MessageFns<UpdateUserRequest> = {
  encode(message: UpdateUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.first_name !== "") {
      writer.uint32(10).string(message.first_name);
    }
    if (message.last_name !== "") {
      writer.uint32(18).string(message.last_name);
    }
    if (message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      writer.uint32(32).int32(userRoleToNumber(message.role));
    }
    writer.uint32(42).fork();
    for (const v of message.team_ids) {
      writer.int64(v);
    }
    writer.join();
    if (message.status !== UserStatus.UNDEFINED_USER_STATUS) {
      writer.uint32(48).int32(userStatusToNumber(message.status));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.first_name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.last_name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.role = userRoleFromJSON(reader.int32());
          continue;
        }
        case 5: {
          if (tag === 40) {
            message.team_ids.push(longToNumber(reader.int64()));

            continue;
          }

          if (tag === 42) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.team_ids.push(longToNumber(reader.int64()));
            }

            continue;
          }

          break;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.status = userStatusFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateUserRequest {
    return {
      first_name: isSet(object.first_name) ? globalThis.String(object.first_name) : "",
      last_name: isSet(object.last_name) ? globalThis.String(object.last_name) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      role: isSet(object.role) ? userRoleFromJSON(object.role) : UserRole.UNDEFINED_USER_ROLE,
      team_ids: globalThis.Array.isArray(object?.team_ids) ? object.team_ids.map((e: any) => globalThis.Number(e)) : [],
      status: isSet(object.status) ? userStatusFromJSON(object.status) : UserStatus.UNDEFINED_USER_STATUS,
    };
  },

  toJSON(message: UpdateUserRequest): unknown {
    const obj: any = {};
    if (message.first_name !== "") {
      obj.first_name = message.first_name;
    }
    if (message.last_name !== "") {
      obj.last_name = message.last_name;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.role !== UserRole.UNDEFINED_USER_ROLE) {
      obj.role = userRoleToJSON(message.role);
    }
    if (message.team_ids?.length) {
      obj.team_ids = message.team_ids.map((e) => Math.round(e));
    }
    if (message.status !== UserStatus.UNDEFINED_USER_STATUS) {
      obj.status = userStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateUserRequest>, I>>(base?: I): UpdateUserRequest {
    return UpdateUserRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateUserRequest>, I>>(object: I): UpdateUserRequest {
    const message = createBaseUpdateUserRequest();
    message.first_name = object.first_name ?? "";
    message.last_name = object.last_name ?? "";
    message.email = object.email ?? "";
    message.role = object.role ?? UserRole.UNDEFINED_USER_ROLE;
    message.team_ids = object.team_ids?.map((e) => e) || [];
    message.status = object.status ?? UserStatus.UNDEFINED_USER_STATUS;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
