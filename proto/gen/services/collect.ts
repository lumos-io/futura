// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.31.1
// source: services/collect.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { KubernetesClusterObjectBatch } from "../cluster/cluster";
import { KubernetesEventBatch } from "../events/events";
import { KubernetesKubeletStats } from "../stats/stats";

export const protobufPackage = "collect";

export interface CollectAck {
  status: string;
  message: string;
}

function createBaseCollectAck(): CollectAck {
  return { status: "", message: "" };
}

export const CollectAck: MessageFns<CollectAck> = {
  encode(message: CollectAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollectAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollectAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollectAck {
    return {
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: CollectAck): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<CollectAck>): CollectAck {
    return CollectAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CollectAck>): CollectAck {
    const message = createBaseCollectAck();
    message.status = object.status ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

export interface CollectService {
  SendEvents(request: KubernetesEventBatch): Promise<CollectAck>;
  SendClusterObjects(request: KubernetesClusterObjectBatch): Promise<CollectAck>;
  SendKubeletMetrics(request: KubernetesKubeletStats): Promise<CollectAck>;
}

export const CollectServiceServiceName = "collect.CollectService";
export class CollectServiceClientImpl implements CollectService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || CollectServiceServiceName;
    this.rpc = rpc;
    this.SendEvents = this.SendEvents.bind(this);
    this.SendClusterObjects = this.SendClusterObjects.bind(this);
    this.SendKubeletMetrics = this.SendKubeletMetrics.bind(this);
  }
  SendEvents(request: KubernetesEventBatch): Promise<CollectAck> {
    const data = KubernetesEventBatch.encode(request).finish();
    const promise = this.rpc.request(this.service, "SendEvents", data);
    return promise.then((data) => CollectAck.decode(new BinaryReader(data)));
  }

  SendClusterObjects(request: KubernetesClusterObjectBatch): Promise<CollectAck> {
    const data = KubernetesClusterObjectBatch.encode(request).finish();
    const promise = this.rpc.request(this.service, "SendClusterObjects", data);
    return promise.then((data) => CollectAck.decode(new BinaryReader(data)));
  }

  SendKubeletMetrics(request: KubernetesKubeletStats): Promise<CollectAck> {
    const data = KubernetesKubeletStats.encode(request).finish();
    const promise = this.rpc.request(this.service, "SendKubeletMetrics", data);
    return promise.then((data) => CollectAck.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
