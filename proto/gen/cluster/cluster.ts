// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.31.1
// source: cluster/cluster.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { APIKey, Metadata } from "../common/metadata";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "cluster";

/** Main batch message */
export interface KubernetesClusterObjectBatch {
  apikey: APIKey | undefined;
  metadata: Metadata | undefined;
  objects: KubernetesClusterObject[];
}

/** Represents a generic Kubernetes object with rich metadata. */
export interface KubernetesClusterObject {
  timestamp:
    | Date
    | undefined;
  /** snapshot | update | delete */
  type: string;
  kind: string;
  namespace: string;
  name: string;
  uid: string;
  labels: { [key: string]: string };
  annotations: { [key: string]: string };
  nodeName: string;
  status: string;
  phase: string;
  restartCount: string;
  ownerKind: string;
  ownerName: string;
  replicas: string;
  readyReplicas: string;
  availableReplicas: string;
  updatedReplicas: string;
  currentReplicas: string;
  containers: ContainerSpec[];
  volumes: VolumeSpec[];
  tolerations: string[];
  affinity: { [key: string]: string };
  extra: { [key: string]: string };
  apiVersion: string;
  /** for HPA */
  hpaMaxReplicas: string;
  hpaMinReplicas: string;
  hpaScaleTargetRef: string;
  /** for Job */
  jobActive: string;
  jobFailed: string;
  jobSucceeded: string;
  jobParallelism: string;
  jobCompletions: string;
  /** for Namespace */
  nsPhaseValue: string;
  /** for Node */
  kubeletVersion: string;
  osType: string;
  osImage: string;
  containerRuntime: string;
  containerRuntimeVersion: string;
  conditions: NodeCondition[];
  allocatable:
    | AllocatableResources
    | undefined;
  /** for Pod */
  podReason: string;
  qosClass: string;
  /** Cluster quota */
  clusterQuota:
    | ClusterResourceQuotaMetadata
    | undefined;
  /** for daemonset */
  daemonsetCurrentNumberScheduled: string;
  daemonsetDesiredNumberScheduled: string;
  daemonsetNumberMisscheduled: string;
  daemonsetNumberReady: string;
}

export interface KubernetesClusterObject_LabelsEntry {
  key: string;
  value: string;
}

export interface KubernetesClusterObject_AnnotationsEntry {
  key: string;
  value: string;
}

export interface KubernetesClusterObject_AffinityEntry {
  key: string;
  value: string;
}

export interface KubernetesClusterObject_ExtraEntry {
  key: string;
  value: string;
}

/** Represents a container and its resource specs. */
export interface ContainerSpec {
  name: string;
  image: string;
  resources: ContainerResources | undefined;
  containerId: string;
  restartsCount: string;
  ready: string;
  state: ContainerState | undefined;
  lastTerminationState: ContainerState | undefined;
  imageTag: string;
}

/** CPU/Memory requests and limits for a container. */
export interface ContainerResources {
  limits: ResourceQuantities | undefined;
  requests: ResourceQuantities | undefined;
}

/** Quantity strings, e.g. "500m", "128Mi" */
export interface ResourceQuantities {
  cpu: string;
  memory: string;
  storage: string;
  ephemeralStorage: string;
}

/** Volumes attached to a pod */
export interface VolumeSpec {
  name: string;
  /** EmptyDir, PVC, ConfigMap, etc. */
  type: string;
}

/** Represents the state of a container: only one of waiting, running, or terminated may be set. */
export interface ContainerState {
  waiting?: ContainerStateWaiting | undefined;
  running?: ContainerStateRunning | undefined;
  terminated?: ContainerStateTerminated | undefined;
}

/** A waiting state of a container (not yet started) */
export interface ContainerStateWaiting {
  reason: string;
  message: string;
}

/** A running state of a container (currently executing) */
export interface ContainerStateRunning {
  startedAt: Date | undefined;
}

/** A terminated state of a container (exited or killed) */
export interface ContainerStateTerminated {
  exitCode: string;
  signal: string;
  reason: string;
  message: string;
  startedAt: Date | undefined;
  finishedAt: Date | undefined;
  containerId: string;
}

export interface NodeCondition {
  /** e.g. "Ready" */
  type: string;
  /** "True", "False", "Unknown" */
  status: string;
  reason: string;
  message: string;
}

export interface AllocatableResources {
  cpu: string;
  memory: string;
  pods: string;
  ephemeralStorage: string;
  others: { [key: string]: string };
}

export interface AllocatableResources_OthersEntry {
  key: string;
  value: string;
}

export interface ClusterResourceQuotaMetadata {
  name: string;
  uid: string;
  totalLimits: QuotaResource[];
  totalUsage: QuotaResource[];
  quotas: NamespaceQuota[];
}

export interface NamespaceQuota {
  namespace: string;
  limits: QuotaResource[];
  usage: QuotaResource[];
}

export interface QuotaResource {
  /** e.g. "cpu", "memory", "count/pods" */
  resource: string;
  /** raw string, like "500m" or "2Gi" */
  value: string;
}

function createBaseKubernetesClusterObjectBatch(): KubernetesClusterObjectBatch {
  return { apikey: undefined, metadata: undefined, objects: [] };
}

export const KubernetesClusterObjectBatch: MessageFns<KubernetesClusterObjectBatch> = {
  encode(message: KubernetesClusterObjectBatch, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apikey !== undefined) {
      APIKey.encode(message.apikey, writer.uint32(10).fork()).join();
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(18).fork()).join();
    }
    for (const v of message.objects) {
      KubernetesClusterObject.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObjectBatch {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObjectBatch();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apikey = APIKey.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.objects.push(KubernetesClusterObject.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObjectBatch {
    return {
      apikey: isSet(object.apikey) ? APIKey.fromJSON(object.apikey) : undefined,
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
      objects: globalThis.Array.isArray(object?.objects)
        ? object.objects.map((e: any) => KubernetesClusterObject.fromJSON(e))
        : [],
    };
  },

  toJSON(message: KubernetesClusterObjectBatch): unknown {
    const obj: any = {};
    if (message.apikey !== undefined) {
      obj.apikey = APIKey.toJSON(message.apikey);
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    if (message.objects?.length) {
      obj.objects = message.objects.map((e) => KubernetesClusterObject.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObjectBatch>): KubernetesClusterObjectBatch {
    return KubernetesClusterObjectBatch.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObjectBatch>): KubernetesClusterObjectBatch {
    const message = createBaseKubernetesClusterObjectBatch();
    message.apikey = (object.apikey !== undefined && object.apikey !== null)
      ? APIKey.fromPartial(object.apikey)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.objects = object.objects?.map((e) => KubernetesClusterObject.fromPartial(e)) || [];
    return message;
  },
};

function createBaseKubernetesClusterObject(): KubernetesClusterObject {
  return {
    timestamp: undefined,
    type: "",
    kind: "",
    namespace: "",
    name: "",
    uid: "",
    labels: {},
    annotations: {},
    nodeName: "",
    status: "",
    phase: "",
    restartCount: "0",
    ownerKind: "",
    ownerName: "",
    replicas: "0",
    readyReplicas: "0",
    availableReplicas: "0",
    updatedReplicas: "0",
    currentReplicas: "0",
    containers: [],
    volumes: [],
    tolerations: [],
    affinity: {},
    extra: {},
    apiVersion: "",
    hpaMaxReplicas: "0",
    hpaMinReplicas: "0",
    hpaScaleTargetRef: "",
    jobActive: "0",
    jobFailed: "0",
    jobSucceeded: "0",
    jobParallelism: "0",
    jobCompletions: "0",
    nsPhaseValue: "0",
    kubeletVersion: "",
    osType: "",
    osImage: "",
    containerRuntime: "",
    containerRuntimeVersion: "",
    conditions: [],
    allocatable: undefined,
    podReason: "",
    qosClass: "",
    clusterQuota: undefined,
    daemonsetCurrentNumberScheduled: "0",
    daemonsetDesiredNumberScheduled: "0",
    daemonsetNumberMisscheduled: "0",
    daemonsetNumberReady: "0",
  };
}

export const KubernetesClusterObject: MessageFns<KubernetesClusterObject> = {
  encode(message: KubernetesClusterObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).join();
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.kind !== "") {
      writer.uint32(26).string(message.kind);
    }
    if (message.namespace !== "") {
      writer.uint32(34).string(message.namespace);
    }
    if (message.name !== "") {
      writer.uint32(42).string(message.name);
    }
    if (message.uid !== "") {
      writer.uint32(50).string(message.uid);
    }
    Object.entries(message.labels).forEach(([key, value]) => {
      KubernetesClusterObject_LabelsEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    Object.entries(message.annotations).forEach(([key, value]) => {
      KubernetesClusterObject_AnnotationsEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    if (message.nodeName !== "") {
      writer.uint32(74).string(message.nodeName);
    }
    if (message.status !== "") {
      writer.uint32(82).string(message.status);
    }
    if (message.phase !== "") {
      writer.uint32(90).string(message.phase);
    }
    if (message.restartCount !== "0") {
      writer.uint32(96).int64(message.restartCount);
    }
    if (message.ownerKind !== "") {
      writer.uint32(106).string(message.ownerKind);
    }
    if (message.ownerName !== "") {
      writer.uint32(114).string(message.ownerName);
    }
    if (message.replicas !== "0") {
      writer.uint32(120).int64(message.replicas);
    }
    if (message.readyReplicas !== "0") {
      writer.uint32(128).int64(message.readyReplicas);
    }
    if (message.availableReplicas !== "0") {
      writer.uint32(136).int64(message.availableReplicas);
    }
    if (message.updatedReplicas !== "0") {
      writer.uint32(144).int64(message.updatedReplicas);
    }
    if (message.currentReplicas !== "0") {
      writer.uint32(344).int64(message.currentReplicas);
    }
    for (const v of message.containers) {
      ContainerSpec.encode(v!, writer.uint32(154).fork()).join();
    }
    for (const v of message.volumes) {
      VolumeSpec.encode(v!, writer.uint32(162).fork()).join();
    }
    for (const v of message.tolerations) {
      writer.uint32(170).string(v!);
    }
    Object.entries(message.affinity).forEach(([key, value]) => {
      KubernetesClusterObject_AffinityEntry.encode({ key: key as any, value }, writer.uint32(178).fork()).join();
    });
    Object.entries(message.extra).forEach(([key, value]) => {
      KubernetesClusterObject_ExtraEntry.encode({ key: key as any, value }, writer.uint32(186).fork()).join();
    });
    if (message.apiVersion !== "") {
      writer.uint32(194).string(message.apiVersion);
    }
    if (message.hpaMaxReplicas !== "0") {
      writer.uint32(200).int64(message.hpaMaxReplicas);
    }
    if (message.hpaMinReplicas !== "0") {
      writer.uint32(208).int64(message.hpaMinReplicas);
    }
    if (message.hpaScaleTargetRef !== "") {
      writer.uint32(218).string(message.hpaScaleTargetRef);
    }
    if (message.jobActive !== "0") {
      writer.uint32(224).int64(message.jobActive);
    }
    if (message.jobFailed !== "0") {
      writer.uint32(232).int64(message.jobFailed);
    }
    if (message.jobSucceeded !== "0") {
      writer.uint32(240).int64(message.jobSucceeded);
    }
    if (message.jobParallelism !== "0") {
      writer.uint32(248).int64(message.jobParallelism);
    }
    if (message.jobCompletions !== "0") {
      writer.uint32(256).int64(message.jobCompletions);
    }
    if (message.nsPhaseValue !== "0") {
      writer.uint32(264).int64(message.nsPhaseValue);
    }
    if (message.kubeletVersion !== "") {
      writer.uint32(274).string(message.kubeletVersion);
    }
    if (message.osType !== "") {
      writer.uint32(282).string(message.osType);
    }
    if (message.osImage !== "") {
      writer.uint32(290).string(message.osImage);
    }
    if (message.containerRuntime !== "") {
      writer.uint32(298).string(message.containerRuntime);
    }
    if (message.containerRuntimeVersion !== "") {
      writer.uint32(306).string(message.containerRuntimeVersion);
    }
    for (const v of message.conditions) {
      NodeCondition.encode(v!, writer.uint32(314).fork()).join();
    }
    if (message.allocatable !== undefined) {
      AllocatableResources.encode(message.allocatable, writer.uint32(322).fork()).join();
    }
    if (message.podReason !== "") {
      writer.uint32(330).string(message.podReason);
    }
    if (message.qosClass !== "") {
      writer.uint32(338).string(message.qosClass);
    }
    if (message.clusterQuota !== undefined) {
      ClusterResourceQuotaMetadata.encode(message.clusterQuota, writer.uint32(354).fork()).join();
    }
    if (message.daemonsetCurrentNumberScheduled !== "0") {
      writer.uint32(360).int64(message.daemonsetCurrentNumberScheduled);
    }
    if (message.daemonsetDesiredNumberScheduled !== "0") {
      writer.uint32(368).int64(message.daemonsetDesiredNumberScheduled);
    }
    if (message.daemonsetNumberMisscheduled !== "0") {
      writer.uint32(376).int64(message.daemonsetNumberMisscheduled);
    }
    if (message.daemonsetNumberReady !== "0") {
      writer.uint32(384).int64(message.daemonsetNumberReady);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.uid = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = KubernetesClusterObject_LabelsEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.labels[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = KubernetesClusterObject_AnnotationsEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.annotations[entry8.key] = entry8.value;
          }
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.nodeName = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.phase = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.restartCount = reader.int64().toString();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.ownerKind = reader.string();
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.ownerName = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 120) {
            break;
          }

          message.replicas = reader.int64().toString();
          continue;
        }
        case 16: {
          if (tag !== 128) {
            break;
          }

          message.readyReplicas = reader.int64().toString();
          continue;
        }
        case 17: {
          if (tag !== 136) {
            break;
          }

          message.availableReplicas = reader.int64().toString();
          continue;
        }
        case 18: {
          if (tag !== 144) {
            break;
          }

          message.updatedReplicas = reader.int64().toString();
          continue;
        }
        case 43: {
          if (tag !== 344) {
            break;
          }

          message.currentReplicas = reader.int64().toString();
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.containers.push(ContainerSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.volumes.push(VolumeSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 21: {
          if (tag !== 170) {
            break;
          }

          message.tolerations.push(reader.string());
          continue;
        }
        case 22: {
          if (tag !== 178) {
            break;
          }

          const entry22 = KubernetesClusterObject_AffinityEntry.decode(reader, reader.uint32());
          if (entry22.value !== undefined) {
            message.affinity[entry22.key] = entry22.value;
          }
          continue;
        }
        case 23: {
          if (tag !== 186) {
            break;
          }

          const entry23 = KubernetesClusterObject_ExtraEntry.decode(reader, reader.uint32());
          if (entry23.value !== undefined) {
            message.extra[entry23.key] = entry23.value;
          }
          continue;
        }
        case 24: {
          if (tag !== 194) {
            break;
          }

          message.apiVersion = reader.string();
          continue;
        }
        case 25: {
          if (tag !== 200) {
            break;
          }

          message.hpaMaxReplicas = reader.int64().toString();
          continue;
        }
        case 26: {
          if (tag !== 208) {
            break;
          }

          message.hpaMinReplicas = reader.int64().toString();
          continue;
        }
        case 27: {
          if (tag !== 218) {
            break;
          }

          message.hpaScaleTargetRef = reader.string();
          continue;
        }
        case 28: {
          if (tag !== 224) {
            break;
          }

          message.jobActive = reader.int64().toString();
          continue;
        }
        case 29: {
          if (tag !== 232) {
            break;
          }

          message.jobFailed = reader.int64().toString();
          continue;
        }
        case 30: {
          if (tag !== 240) {
            break;
          }

          message.jobSucceeded = reader.int64().toString();
          continue;
        }
        case 31: {
          if (tag !== 248) {
            break;
          }

          message.jobParallelism = reader.int64().toString();
          continue;
        }
        case 32: {
          if (tag !== 256) {
            break;
          }

          message.jobCompletions = reader.int64().toString();
          continue;
        }
        case 33: {
          if (tag !== 264) {
            break;
          }

          message.nsPhaseValue = reader.int64().toString();
          continue;
        }
        case 34: {
          if (tag !== 274) {
            break;
          }

          message.kubeletVersion = reader.string();
          continue;
        }
        case 35: {
          if (tag !== 282) {
            break;
          }

          message.osType = reader.string();
          continue;
        }
        case 36: {
          if (tag !== 290) {
            break;
          }

          message.osImage = reader.string();
          continue;
        }
        case 37: {
          if (tag !== 298) {
            break;
          }

          message.containerRuntime = reader.string();
          continue;
        }
        case 38: {
          if (tag !== 306) {
            break;
          }

          message.containerRuntimeVersion = reader.string();
          continue;
        }
        case 39: {
          if (tag !== 314) {
            break;
          }

          message.conditions.push(NodeCondition.decode(reader, reader.uint32()));
          continue;
        }
        case 40: {
          if (tag !== 322) {
            break;
          }

          message.allocatable = AllocatableResources.decode(reader, reader.uint32());
          continue;
        }
        case 41: {
          if (tag !== 330) {
            break;
          }

          message.podReason = reader.string();
          continue;
        }
        case 42: {
          if (tag !== 338) {
            break;
          }

          message.qosClass = reader.string();
          continue;
        }
        case 44: {
          if (tag !== 354) {
            break;
          }

          message.clusterQuota = ClusterResourceQuotaMetadata.decode(reader, reader.uint32());
          continue;
        }
        case 45: {
          if (tag !== 360) {
            break;
          }

          message.daemonsetCurrentNumberScheduled = reader.int64().toString();
          continue;
        }
        case 46: {
          if (tag !== 368) {
            break;
          }

          message.daemonsetDesiredNumberScheduled = reader.int64().toString();
          continue;
        }
        case 47: {
          if (tag !== 376) {
            break;
          }

          message.daemonsetNumberMisscheduled = reader.int64().toString();
          continue;
        }
        case 48: {
          if (tag !== 384) {
            break;
          }

          message.daemonsetNumberReady = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObject {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      uid: isSet(object.uid) ? globalThis.String(object.uid) : "",
      labels: isObject(object.labels)
        ? Object.entries(object.labels).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      annotations: isObject(object.annotations)
        ? Object.entries(object.annotations).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      nodeName: isSet(object.nodeName) ? globalThis.String(object.nodeName) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      phase: isSet(object.phase) ? globalThis.String(object.phase) : "",
      restartCount: isSet(object.restartCount) ? globalThis.String(object.restartCount) : "0",
      ownerKind: isSet(object.ownerKind) ? globalThis.String(object.ownerKind) : "",
      ownerName: isSet(object.ownerName) ? globalThis.String(object.ownerName) : "",
      replicas: isSet(object.replicas) ? globalThis.String(object.replicas) : "0",
      readyReplicas: isSet(object.readyReplicas) ? globalThis.String(object.readyReplicas) : "0",
      availableReplicas: isSet(object.availableReplicas) ? globalThis.String(object.availableReplicas) : "0",
      updatedReplicas: isSet(object.updatedReplicas) ? globalThis.String(object.updatedReplicas) : "0",
      currentReplicas: isSet(object.currentReplicas) ? globalThis.String(object.currentReplicas) : "0",
      containers: globalThis.Array.isArray(object?.containers)
        ? object.containers.map((e: any) => ContainerSpec.fromJSON(e))
        : [],
      volumes: globalThis.Array.isArray(object?.volumes)
        ? object.volumes.map((e: any) => VolumeSpec.fromJSON(e))
        : [],
      tolerations: globalThis.Array.isArray(object?.tolerations)
        ? object.tolerations.map((e: any) => globalThis.String(e))
        : [],
      affinity: isObject(object.affinity)
        ? Object.entries(object.affinity).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      extra: isObject(object.extra)
        ? Object.entries(object.extra).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      apiVersion: isSet(object.apiVersion) ? globalThis.String(object.apiVersion) : "",
      hpaMaxReplicas: isSet(object.hpaMaxReplicas) ? globalThis.String(object.hpaMaxReplicas) : "0",
      hpaMinReplicas: isSet(object.hpaMinReplicas) ? globalThis.String(object.hpaMinReplicas) : "0",
      hpaScaleTargetRef: isSet(object.hpaScaleTargetRef) ? globalThis.String(object.hpaScaleTargetRef) : "",
      jobActive: isSet(object.jobActive) ? globalThis.String(object.jobActive) : "0",
      jobFailed: isSet(object.jobFailed) ? globalThis.String(object.jobFailed) : "0",
      jobSucceeded: isSet(object.jobSucceeded) ? globalThis.String(object.jobSucceeded) : "0",
      jobParallelism: isSet(object.jobParallelism) ? globalThis.String(object.jobParallelism) : "0",
      jobCompletions: isSet(object.jobCompletions) ? globalThis.String(object.jobCompletions) : "0",
      nsPhaseValue: isSet(object.nsPhaseValue) ? globalThis.String(object.nsPhaseValue) : "0",
      kubeletVersion: isSet(object.kubeletVersion) ? globalThis.String(object.kubeletVersion) : "",
      osType: isSet(object.osType) ? globalThis.String(object.osType) : "",
      osImage: isSet(object.osImage) ? globalThis.String(object.osImage) : "",
      containerRuntime: isSet(object.containerRuntime) ? globalThis.String(object.containerRuntime) : "",
      containerRuntimeVersion: isSet(object.containerRuntimeVersion)
        ? globalThis.String(object.containerRuntimeVersion)
        : "",
      conditions: globalThis.Array.isArray(object?.conditions)
        ? object.conditions.map((e: any) => NodeCondition.fromJSON(e))
        : [],
      allocatable: isSet(object.allocatable) ? AllocatableResources.fromJSON(object.allocatable) : undefined,
      podReason: isSet(object.podReason) ? globalThis.String(object.podReason) : "",
      qosClass: isSet(object.qosClass) ? globalThis.String(object.qosClass) : "",
      clusterQuota: isSet(object.clusterQuota) ? ClusterResourceQuotaMetadata.fromJSON(object.clusterQuota) : undefined,
      daemonsetCurrentNumberScheduled: isSet(object.daemonsetCurrentNumberScheduled)
        ? globalThis.String(object.daemonsetCurrentNumberScheduled)
        : "0",
      daemonsetDesiredNumberScheduled: isSet(object.daemonsetDesiredNumberScheduled)
        ? globalThis.String(object.daemonsetDesiredNumberScheduled)
        : "0",
      daemonsetNumberMisscheduled: isSet(object.daemonsetNumberMisscheduled)
        ? globalThis.String(object.daemonsetNumberMisscheduled)
        : "0",
      daemonsetNumberReady: isSet(object.daemonsetNumberReady) ? globalThis.String(object.daemonsetNumberReady) : "0",
    };
  },

  toJSON(message: KubernetesClusterObject): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.uid !== "") {
      obj.uid = message.uid;
    }
    if (message.labels) {
      const entries = Object.entries(message.labels);
      if (entries.length > 0) {
        obj.labels = {};
        entries.forEach(([k, v]) => {
          obj.labels[k] = v;
        });
      }
    }
    if (message.annotations) {
      const entries = Object.entries(message.annotations);
      if (entries.length > 0) {
        obj.annotations = {};
        entries.forEach(([k, v]) => {
          obj.annotations[k] = v;
        });
      }
    }
    if (message.nodeName !== "") {
      obj.nodeName = message.nodeName;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.phase !== "") {
      obj.phase = message.phase;
    }
    if (message.restartCount !== "0") {
      obj.restartCount = message.restartCount;
    }
    if (message.ownerKind !== "") {
      obj.ownerKind = message.ownerKind;
    }
    if (message.ownerName !== "") {
      obj.ownerName = message.ownerName;
    }
    if (message.replicas !== "0") {
      obj.replicas = message.replicas;
    }
    if (message.readyReplicas !== "0") {
      obj.readyReplicas = message.readyReplicas;
    }
    if (message.availableReplicas !== "0") {
      obj.availableReplicas = message.availableReplicas;
    }
    if (message.updatedReplicas !== "0") {
      obj.updatedReplicas = message.updatedReplicas;
    }
    if (message.currentReplicas !== "0") {
      obj.currentReplicas = message.currentReplicas;
    }
    if (message.containers?.length) {
      obj.containers = message.containers.map((e) => ContainerSpec.toJSON(e));
    }
    if (message.volumes?.length) {
      obj.volumes = message.volumes.map((e) => VolumeSpec.toJSON(e));
    }
    if (message.tolerations?.length) {
      obj.tolerations = message.tolerations;
    }
    if (message.affinity) {
      const entries = Object.entries(message.affinity);
      if (entries.length > 0) {
        obj.affinity = {};
        entries.forEach(([k, v]) => {
          obj.affinity[k] = v;
        });
      }
    }
    if (message.extra) {
      const entries = Object.entries(message.extra);
      if (entries.length > 0) {
        obj.extra = {};
        entries.forEach(([k, v]) => {
          obj.extra[k] = v;
        });
      }
    }
    if (message.apiVersion !== "") {
      obj.apiVersion = message.apiVersion;
    }
    if (message.hpaMaxReplicas !== "0") {
      obj.hpaMaxReplicas = message.hpaMaxReplicas;
    }
    if (message.hpaMinReplicas !== "0") {
      obj.hpaMinReplicas = message.hpaMinReplicas;
    }
    if (message.hpaScaleTargetRef !== "") {
      obj.hpaScaleTargetRef = message.hpaScaleTargetRef;
    }
    if (message.jobActive !== "0") {
      obj.jobActive = message.jobActive;
    }
    if (message.jobFailed !== "0") {
      obj.jobFailed = message.jobFailed;
    }
    if (message.jobSucceeded !== "0") {
      obj.jobSucceeded = message.jobSucceeded;
    }
    if (message.jobParallelism !== "0") {
      obj.jobParallelism = message.jobParallelism;
    }
    if (message.jobCompletions !== "0") {
      obj.jobCompletions = message.jobCompletions;
    }
    if (message.nsPhaseValue !== "0") {
      obj.nsPhaseValue = message.nsPhaseValue;
    }
    if (message.kubeletVersion !== "") {
      obj.kubeletVersion = message.kubeletVersion;
    }
    if (message.osType !== "") {
      obj.osType = message.osType;
    }
    if (message.osImage !== "") {
      obj.osImage = message.osImage;
    }
    if (message.containerRuntime !== "") {
      obj.containerRuntime = message.containerRuntime;
    }
    if (message.containerRuntimeVersion !== "") {
      obj.containerRuntimeVersion = message.containerRuntimeVersion;
    }
    if (message.conditions?.length) {
      obj.conditions = message.conditions.map((e) => NodeCondition.toJSON(e));
    }
    if (message.allocatable !== undefined) {
      obj.allocatable = AllocatableResources.toJSON(message.allocatable);
    }
    if (message.podReason !== "") {
      obj.podReason = message.podReason;
    }
    if (message.qosClass !== "") {
      obj.qosClass = message.qosClass;
    }
    if (message.clusterQuota !== undefined) {
      obj.clusterQuota = ClusterResourceQuotaMetadata.toJSON(message.clusterQuota);
    }
    if (message.daemonsetCurrentNumberScheduled !== "0") {
      obj.daemonsetCurrentNumberScheduled = message.daemonsetCurrentNumberScheduled;
    }
    if (message.daemonsetDesiredNumberScheduled !== "0") {
      obj.daemonsetDesiredNumberScheduled = message.daemonsetDesiredNumberScheduled;
    }
    if (message.daemonsetNumberMisscheduled !== "0") {
      obj.daemonsetNumberMisscheduled = message.daemonsetNumberMisscheduled;
    }
    if (message.daemonsetNumberReady !== "0") {
      obj.daemonsetNumberReady = message.daemonsetNumberReady;
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObject>): KubernetesClusterObject {
    return KubernetesClusterObject.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObject>): KubernetesClusterObject {
    const message = createBaseKubernetesClusterObject();
    message.timestamp = object.timestamp ?? undefined;
    message.type = object.type ?? "";
    message.kind = object.kind ?? "";
    message.namespace = object.namespace ?? "";
    message.name = object.name ?? "";
    message.uid = object.uid ?? "";
    message.labels = Object.entries(object.labels ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.annotations = Object.entries(object.annotations ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.nodeName = object.nodeName ?? "";
    message.status = object.status ?? "";
    message.phase = object.phase ?? "";
    message.restartCount = object.restartCount ?? "0";
    message.ownerKind = object.ownerKind ?? "";
    message.ownerName = object.ownerName ?? "";
    message.replicas = object.replicas ?? "0";
    message.readyReplicas = object.readyReplicas ?? "0";
    message.availableReplicas = object.availableReplicas ?? "0";
    message.updatedReplicas = object.updatedReplicas ?? "0";
    message.currentReplicas = object.currentReplicas ?? "0";
    message.containers = object.containers?.map((e) => ContainerSpec.fromPartial(e)) || [];
    message.volumes = object.volumes?.map((e) => VolumeSpec.fromPartial(e)) || [];
    message.tolerations = object.tolerations?.map((e) => e) || [];
    message.affinity = Object.entries(object.affinity ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.extra = Object.entries(object.extra ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.apiVersion = object.apiVersion ?? "";
    message.hpaMaxReplicas = object.hpaMaxReplicas ?? "0";
    message.hpaMinReplicas = object.hpaMinReplicas ?? "0";
    message.hpaScaleTargetRef = object.hpaScaleTargetRef ?? "";
    message.jobActive = object.jobActive ?? "0";
    message.jobFailed = object.jobFailed ?? "0";
    message.jobSucceeded = object.jobSucceeded ?? "0";
    message.jobParallelism = object.jobParallelism ?? "0";
    message.jobCompletions = object.jobCompletions ?? "0";
    message.nsPhaseValue = object.nsPhaseValue ?? "0";
    message.kubeletVersion = object.kubeletVersion ?? "";
    message.osType = object.osType ?? "";
    message.osImage = object.osImage ?? "";
    message.containerRuntime = object.containerRuntime ?? "";
    message.containerRuntimeVersion = object.containerRuntimeVersion ?? "";
    message.conditions = object.conditions?.map((e) => NodeCondition.fromPartial(e)) || [];
    message.allocatable = (object.allocatable !== undefined && object.allocatable !== null)
      ? AllocatableResources.fromPartial(object.allocatable)
      : undefined;
    message.podReason = object.podReason ?? "";
    message.qosClass = object.qosClass ?? "";
    message.clusterQuota = (object.clusterQuota !== undefined && object.clusterQuota !== null)
      ? ClusterResourceQuotaMetadata.fromPartial(object.clusterQuota)
      : undefined;
    message.daemonsetCurrentNumberScheduled = object.daemonsetCurrentNumberScheduled ?? "0";
    message.daemonsetDesiredNumberScheduled = object.daemonsetDesiredNumberScheduled ?? "0";
    message.daemonsetNumberMisscheduled = object.daemonsetNumberMisscheduled ?? "0";
    message.daemonsetNumberReady = object.daemonsetNumberReady ?? "0";
    return message;
  },
};

function createBaseKubernetesClusterObject_LabelsEntry(): KubernetesClusterObject_LabelsEntry {
  return { key: "", value: "" };
}

export const KubernetesClusterObject_LabelsEntry: MessageFns<KubernetesClusterObject_LabelsEntry> = {
  encode(message: KubernetesClusterObject_LabelsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObject_LabelsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObject_LabelsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObject_LabelsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KubernetesClusterObject_LabelsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObject_LabelsEntry>): KubernetesClusterObject_LabelsEntry {
    return KubernetesClusterObject_LabelsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObject_LabelsEntry>): KubernetesClusterObject_LabelsEntry {
    const message = createBaseKubernetesClusterObject_LabelsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseKubernetesClusterObject_AnnotationsEntry(): KubernetesClusterObject_AnnotationsEntry {
  return { key: "", value: "" };
}

export const KubernetesClusterObject_AnnotationsEntry: MessageFns<KubernetesClusterObject_AnnotationsEntry> = {
  encode(message: KubernetesClusterObject_AnnotationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObject_AnnotationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObject_AnnotationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObject_AnnotationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KubernetesClusterObject_AnnotationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObject_AnnotationsEntry>): KubernetesClusterObject_AnnotationsEntry {
    return KubernetesClusterObject_AnnotationsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObject_AnnotationsEntry>): KubernetesClusterObject_AnnotationsEntry {
    const message = createBaseKubernetesClusterObject_AnnotationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseKubernetesClusterObject_AffinityEntry(): KubernetesClusterObject_AffinityEntry {
  return { key: "", value: "" };
}

export const KubernetesClusterObject_AffinityEntry: MessageFns<KubernetesClusterObject_AffinityEntry> = {
  encode(message: KubernetesClusterObject_AffinityEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObject_AffinityEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObject_AffinityEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObject_AffinityEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KubernetesClusterObject_AffinityEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObject_AffinityEntry>): KubernetesClusterObject_AffinityEntry {
    return KubernetesClusterObject_AffinityEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObject_AffinityEntry>): KubernetesClusterObject_AffinityEntry {
    const message = createBaseKubernetesClusterObject_AffinityEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseKubernetesClusterObject_ExtraEntry(): KubernetesClusterObject_ExtraEntry {
  return { key: "", value: "" };
}

export const KubernetesClusterObject_ExtraEntry: MessageFns<KubernetesClusterObject_ExtraEntry> = {
  encode(message: KubernetesClusterObject_ExtraEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KubernetesClusterObject_ExtraEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKubernetesClusterObject_ExtraEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KubernetesClusterObject_ExtraEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: KubernetesClusterObject_ExtraEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<KubernetesClusterObject_ExtraEntry>): KubernetesClusterObject_ExtraEntry {
    return KubernetesClusterObject_ExtraEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KubernetesClusterObject_ExtraEntry>): KubernetesClusterObject_ExtraEntry {
    const message = createBaseKubernetesClusterObject_ExtraEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseContainerSpec(): ContainerSpec {
  return {
    name: "",
    image: "",
    resources: undefined,
    containerId: "",
    restartsCount: "0",
    ready: "0",
    state: undefined,
    lastTerminationState: undefined,
    imageTag: "",
  };
}

export const ContainerSpec: MessageFns<ContainerSpec> = {
  encode(message: ContainerSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.image !== "") {
      writer.uint32(18).string(message.image);
    }
    if (message.resources !== undefined) {
      ContainerResources.encode(message.resources, writer.uint32(26).fork()).join();
    }
    if (message.containerId !== "") {
      writer.uint32(34).string(message.containerId);
    }
    if (message.restartsCount !== "0") {
      writer.uint32(40).int64(message.restartsCount);
    }
    if (message.ready !== "0") {
      writer.uint32(48).int64(message.ready);
    }
    if (message.state !== undefined) {
      ContainerState.encode(message.state, writer.uint32(58).fork()).join();
    }
    if (message.lastTerminationState !== undefined) {
      ContainerState.encode(message.lastTerminationState, writer.uint32(66).fork()).join();
    }
    if (message.imageTag !== "") {
      writer.uint32(74).string(message.imageTag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.resources = ContainerResources.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.restartsCount = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.ready = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.state = ContainerState.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastTerminationState = ContainerState.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.imageTag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerSpec {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      image: isSet(object.image) ? globalThis.String(object.image) : "",
      resources: isSet(object.resources) ? ContainerResources.fromJSON(object.resources) : undefined,
      containerId: isSet(object.containerId) ? globalThis.String(object.containerId) : "",
      restartsCount: isSet(object.restartsCount) ? globalThis.String(object.restartsCount) : "0",
      ready: isSet(object.ready) ? globalThis.String(object.ready) : "0",
      state: isSet(object.state) ? ContainerState.fromJSON(object.state) : undefined,
      lastTerminationState: isSet(object.lastTerminationState)
        ? ContainerState.fromJSON(object.lastTerminationState)
        : undefined,
      imageTag: isSet(object.imageTag) ? globalThis.String(object.imageTag) : "",
    };
  },

  toJSON(message: ContainerSpec): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.image !== "") {
      obj.image = message.image;
    }
    if (message.resources !== undefined) {
      obj.resources = ContainerResources.toJSON(message.resources);
    }
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    if (message.restartsCount !== "0") {
      obj.restartsCount = message.restartsCount;
    }
    if (message.ready !== "0") {
      obj.ready = message.ready;
    }
    if (message.state !== undefined) {
      obj.state = ContainerState.toJSON(message.state);
    }
    if (message.lastTerminationState !== undefined) {
      obj.lastTerminationState = ContainerState.toJSON(message.lastTerminationState);
    }
    if (message.imageTag !== "") {
      obj.imageTag = message.imageTag;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerSpec>): ContainerSpec {
    return ContainerSpec.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerSpec>): ContainerSpec {
    const message = createBaseContainerSpec();
    message.name = object.name ?? "";
    message.image = object.image ?? "";
    message.resources = (object.resources !== undefined && object.resources !== null)
      ? ContainerResources.fromPartial(object.resources)
      : undefined;
    message.containerId = object.containerId ?? "";
    message.restartsCount = object.restartsCount ?? "0";
    message.ready = object.ready ?? "0";
    message.state = (object.state !== undefined && object.state !== null)
      ? ContainerState.fromPartial(object.state)
      : undefined;
    message.lastTerminationState = (object.lastTerminationState !== undefined && object.lastTerminationState !== null)
      ? ContainerState.fromPartial(object.lastTerminationState)
      : undefined;
    message.imageTag = object.imageTag ?? "";
    return message;
  },
};

function createBaseContainerResources(): ContainerResources {
  return { limits: undefined, requests: undefined };
}

export const ContainerResources: MessageFns<ContainerResources> = {
  encode(message: ContainerResources, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.limits !== undefined) {
      ResourceQuantities.encode(message.limits, writer.uint32(10).fork()).join();
    }
    if (message.requests !== undefined) {
      ResourceQuantities.encode(message.requests, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerResources {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerResources();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.limits = ResourceQuantities.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.requests = ResourceQuantities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerResources {
    return {
      limits: isSet(object.limits) ? ResourceQuantities.fromJSON(object.limits) : undefined,
      requests: isSet(object.requests) ? ResourceQuantities.fromJSON(object.requests) : undefined,
    };
  },

  toJSON(message: ContainerResources): unknown {
    const obj: any = {};
    if (message.limits !== undefined) {
      obj.limits = ResourceQuantities.toJSON(message.limits);
    }
    if (message.requests !== undefined) {
      obj.requests = ResourceQuantities.toJSON(message.requests);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerResources>): ContainerResources {
    return ContainerResources.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerResources>): ContainerResources {
    const message = createBaseContainerResources();
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? ResourceQuantities.fromPartial(object.limits)
      : undefined;
    message.requests = (object.requests !== undefined && object.requests !== null)
      ? ResourceQuantities.fromPartial(object.requests)
      : undefined;
    return message;
  },
};

function createBaseResourceQuantities(): ResourceQuantities {
  return { cpu: "", memory: "", storage: "", ephemeralStorage: "" };
}

export const ResourceQuantities: MessageFns<ResourceQuantities> = {
  encode(message: ResourceQuantities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cpu !== "") {
      writer.uint32(10).string(message.cpu);
    }
    if (message.memory !== "") {
      writer.uint32(18).string(message.memory);
    }
    if (message.storage !== "") {
      writer.uint32(26).string(message.storage);
    }
    if (message.ephemeralStorage !== "") {
      writer.uint32(34).string(message.ephemeralStorage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResourceQuantities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResourceQuantities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cpu = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.memory = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.storage = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ephemeralStorage = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResourceQuantities {
    return {
      cpu: isSet(object.cpu) ? globalThis.String(object.cpu) : "",
      memory: isSet(object.memory) ? globalThis.String(object.memory) : "",
      storage: isSet(object.storage) ? globalThis.String(object.storage) : "",
      ephemeralStorage: isSet(object.ephemeralStorage) ? globalThis.String(object.ephemeralStorage) : "",
    };
  },

  toJSON(message: ResourceQuantities): unknown {
    const obj: any = {};
    if (message.cpu !== "") {
      obj.cpu = message.cpu;
    }
    if (message.memory !== "") {
      obj.memory = message.memory;
    }
    if (message.storage !== "") {
      obj.storage = message.storage;
    }
    if (message.ephemeralStorage !== "") {
      obj.ephemeralStorage = message.ephemeralStorage;
    }
    return obj;
  },

  create(base?: DeepPartial<ResourceQuantities>): ResourceQuantities {
    return ResourceQuantities.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResourceQuantities>): ResourceQuantities {
    const message = createBaseResourceQuantities();
    message.cpu = object.cpu ?? "";
    message.memory = object.memory ?? "";
    message.storage = object.storage ?? "";
    message.ephemeralStorage = object.ephemeralStorage ?? "";
    return message;
  },
};

function createBaseVolumeSpec(): VolumeSpec {
  return { name: "", type: "" };
}

export const VolumeSpec: MessageFns<VolumeSpec> = {
  encode(message: VolumeSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VolumeSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVolumeSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VolumeSpec {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
    };
  },

  toJSON(message: VolumeSpec): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    return obj;
  },

  create(base?: DeepPartial<VolumeSpec>): VolumeSpec {
    return VolumeSpec.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VolumeSpec>): VolumeSpec {
    const message = createBaseVolumeSpec();
    message.name = object.name ?? "";
    message.type = object.type ?? "";
    return message;
  },
};

function createBaseContainerState(): ContainerState {
  return { waiting: undefined, running: undefined, terminated: undefined };
}

export const ContainerState: MessageFns<ContainerState> = {
  encode(message: ContainerState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.waiting !== undefined) {
      ContainerStateWaiting.encode(message.waiting, writer.uint32(10).fork()).join();
    }
    if (message.running !== undefined) {
      ContainerStateRunning.encode(message.running, writer.uint32(18).fork()).join();
    }
    if (message.terminated !== undefined) {
      ContainerStateTerminated.encode(message.terminated, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.waiting = ContainerStateWaiting.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.running = ContainerStateRunning.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.terminated = ContainerStateTerminated.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerState {
    return {
      waiting: isSet(object.waiting) ? ContainerStateWaiting.fromJSON(object.waiting) : undefined,
      running: isSet(object.running) ? ContainerStateRunning.fromJSON(object.running) : undefined,
      terminated: isSet(object.terminated) ? ContainerStateTerminated.fromJSON(object.terminated) : undefined,
    };
  },

  toJSON(message: ContainerState): unknown {
    const obj: any = {};
    if (message.waiting !== undefined) {
      obj.waiting = ContainerStateWaiting.toJSON(message.waiting);
    }
    if (message.running !== undefined) {
      obj.running = ContainerStateRunning.toJSON(message.running);
    }
    if (message.terminated !== undefined) {
      obj.terminated = ContainerStateTerminated.toJSON(message.terminated);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerState>): ContainerState {
    return ContainerState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerState>): ContainerState {
    const message = createBaseContainerState();
    message.waiting = (object.waiting !== undefined && object.waiting !== null)
      ? ContainerStateWaiting.fromPartial(object.waiting)
      : undefined;
    message.running = (object.running !== undefined && object.running !== null)
      ? ContainerStateRunning.fromPartial(object.running)
      : undefined;
    message.terminated = (object.terminated !== undefined && object.terminated !== null)
      ? ContainerStateTerminated.fromPartial(object.terminated)
      : undefined;
    return message;
  },
};

function createBaseContainerStateWaiting(): ContainerStateWaiting {
  return { reason: "", message: "" };
}

export const ContainerStateWaiting: MessageFns<ContainerStateWaiting> = {
  encode(message: ContainerStateWaiting, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== "") {
      writer.uint32(10).string(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStateWaiting {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStateWaiting();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStateWaiting {
    return {
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ContainerStateWaiting): unknown {
    const obj: any = {};
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStateWaiting>): ContainerStateWaiting {
    return ContainerStateWaiting.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStateWaiting>): ContainerStateWaiting {
    const message = createBaseContainerStateWaiting();
    message.reason = object.reason ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseContainerStateRunning(): ContainerStateRunning {
  return { startedAt: undefined };
}

export const ContainerStateRunning: MessageFns<ContainerStateRunning> = {
  encode(message: ContainerStateRunning, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStateRunning {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStateRunning();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStateRunning {
    return { startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined };
  },

  toJSON(message: ContainerStateRunning): unknown {
    const obj: any = {};
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStateRunning>): ContainerStateRunning {
    return ContainerStateRunning.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStateRunning>): ContainerStateRunning {
    const message = createBaseContainerStateRunning();
    message.startedAt = object.startedAt ?? undefined;
    return message;
  },
};

function createBaseContainerStateTerminated(): ContainerStateTerminated {
  return {
    exitCode: "0",
    signal: "0",
    reason: "",
    message: "",
    startedAt: undefined,
    finishedAt: undefined,
    containerId: "",
  };
}

export const ContainerStateTerminated: MessageFns<ContainerStateTerminated> = {
  encode(message: ContainerStateTerminated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== "0") {
      writer.uint32(8).int64(message.exitCode);
    }
    if (message.signal !== "0") {
      writer.uint32(16).int64(message.signal);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(42).fork()).join();
    }
    if (message.finishedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.finishedAt), writer.uint32(50).fork()).join();
    }
    if (message.containerId !== "") {
      writer.uint32(58).string(message.containerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerStateTerminated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerStateTerminated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.signal = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.finishedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.containerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerStateTerminated {
    return {
      exitCode: isSet(object.exitCode) ? globalThis.String(object.exitCode) : "0",
      signal: isSet(object.signal) ? globalThis.String(object.signal) : "0",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      startedAt: isSet(object.startedAt) ? fromJsonTimestamp(object.startedAt) : undefined,
      finishedAt: isSet(object.finishedAt) ? fromJsonTimestamp(object.finishedAt) : undefined,
      containerId: isSet(object.containerId) ? globalThis.String(object.containerId) : "",
    };
  },

  toJSON(message: ContainerStateTerminated): unknown {
    const obj: any = {};
    if (message.exitCode !== "0") {
      obj.exitCode = message.exitCode;
    }
    if (message.signal !== "0") {
      obj.signal = message.signal;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.finishedAt !== undefined) {
      obj.finishedAt = message.finishedAt.toISOString();
    }
    if (message.containerId !== "") {
      obj.containerId = message.containerId;
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerStateTerminated>): ContainerStateTerminated {
    return ContainerStateTerminated.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerStateTerminated>): ContainerStateTerminated {
    const message = createBaseContainerStateTerminated();
    message.exitCode = object.exitCode ?? "0";
    message.signal = object.signal ?? "0";
    message.reason = object.reason ?? "";
    message.message = object.message ?? "";
    message.startedAt = object.startedAt ?? undefined;
    message.finishedAt = object.finishedAt ?? undefined;
    message.containerId = object.containerId ?? "";
    return message;
  },
};

function createBaseNodeCondition(): NodeCondition {
  return { type: "", status: "", reason: "", message: "" };
}

export const NodeCondition: MessageFns<NodeCondition> = {
  encode(message: NodeCondition, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.message !== "") {
      writer.uint32(34).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeCondition {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeCondition();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeCondition {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: NodeCondition): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<NodeCondition>): NodeCondition {
    return NodeCondition.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NodeCondition>): NodeCondition {
    const message = createBaseNodeCondition();
    message.type = object.type ?? "";
    message.status = object.status ?? "";
    message.reason = object.reason ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseAllocatableResources(): AllocatableResources {
  return { cpu: "", memory: "", pods: "", ephemeralStorage: "", others: {} };
}

export const AllocatableResources: MessageFns<AllocatableResources> = {
  encode(message: AllocatableResources, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.cpu !== "") {
      writer.uint32(10).string(message.cpu);
    }
    if (message.memory !== "") {
      writer.uint32(18).string(message.memory);
    }
    if (message.pods !== "") {
      writer.uint32(26).string(message.pods);
    }
    if (message.ephemeralStorage !== "") {
      writer.uint32(34).string(message.ephemeralStorage);
    }
    Object.entries(message.others).forEach(([key, value]) => {
      AllocatableResources_OthersEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocatableResources {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocatableResources();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.cpu = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.memory = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pods = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ephemeralStorage = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = AllocatableResources_OthersEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.others[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocatableResources {
    return {
      cpu: isSet(object.cpu) ? globalThis.String(object.cpu) : "",
      memory: isSet(object.memory) ? globalThis.String(object.memory) : "",
      pods: isSet(object.pods) ? globalThis.String(object.pods) : "",
      ephemeralStorage: isSet(object.ephemeralStorage) ? globalThis.String(object.ephemeralStorage) : "",
      others: isObject(object.others)
        ? Object.entries(object.others).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: AllocatableResources): unknown {
    const obj: any = {};
    if (message.cpu !== "") {
      obj.cpu = message.cpu;
    }
    if (message.memory !== "") {
      obj.memory = message.memory;
    }
    if (message.pods !== "") {
      obj.pods = message.pods;
    }
    if (message.ephemeralStorage !== "") {
      obj.ephemeralStorage = message.ephemeralStorage;
    }
    if (message.others) {
      const entries = Object.entries(message.others);
      if (entries.length > 0) {
        obj.others = {};
        entries.forEach(([k, v]) => {
          obj.others[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<AllocatableResources>): AllocatableResources {
    return AllocatableResources.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AllocatableResources>): AllocatableResources {
    const message = createBaseAllocatableResources();
    message.cpu = object.cpu ?? "";
    message.memory = object.memory ?? "";
    message.pods = object.pods ?? "";
    message.ephemeralStorage = object.ephemeralStorage ?? "";
    message.others = Object.entries(object.others ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseAllocatableResources_OthersEntry(): AllocatableResources_OthersEntry {
  return { key: "", value: "" };
}

export const AllocatableResources_OthersEntry: MessageFns<AllocatableResources_OthersEntry> = {
  encode(message: AllocatableResources_OthersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AllocatableResources_OthersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAllocatableResources_OthersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AllocatableResources_OthersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AllocatableResources_OthersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<AllocatableResources_OthersEntry>): AllocatableResources_OthersEntry {
    return AllocatableResources_OthersEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AllocatableResources_OthersEntry>): AllocatableResources_OthersEntry {
    const message = createBaseAllocatableResources_OthersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseClusterResourceQuotaMetadata(): ClusterResourceQuotaMetadata {
  return { name: "", uid: "", totalLimits: [], totalUsage: [], quotas: [] };
}

export const ClusterResourceQuotaMetadata: MessageFns<ClusterResourceQuotaMetadata> = {
  encode(message: ClusterResourceQuotaMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.uid !== "") {
      writer.uint32(18).string(message.uid);
    }
    for (const v of message.totalLimits) {
      QuotaResource.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.totalUsage) {
      QuotaResource.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.quotas) {
      NamespaceQuota.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClusterResourceQuotaMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClusterResourceQuotaMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.totalLimits.push(QuotaResource.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.totalUsage.push(QuotaResource.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.quotas.push(NamespaceQuota.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClusterResourceQuotaMetadata {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      uid: isSet(object.uid) ? globalThis.String(object.uid) : "",
      totalLimits: globalThis.Array.isArray(object?.totalLimits)
        ? object.totalLimits.map((e: any) => QuotaResource.fromJSON(e))
        : [],
      totalUsage: globalThis.Array.isArray(object?.totalUsage)
        ? object.totalUsage.map((e: any) => QuotaResource.fromJSON(e))
        : [],
      quotas: globalThis.Array.isArray(object?.quotas) ? object.quotas.map((e: any) => NamespaceQuota.fromJSON(e)) : [],
    };
  },

  toJSON(message: ClusterResourceQuotaMetadata): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.uid !== "") {
      obj.uid = message.uid;
    }
    if (message.totalLimits?.length) {
      obj.totalLimits = message.totalLimits.map((e) => QuotaResource.toJSON(e));
    }
    if (message.totalUsage?.length) {
      obj.totalUsage = message.totalUsage.map((e) => QuotaResource.toJSON(e));
    }
    if (message.quotas?.length) {
      obj.quotas = message.quotas.map((e) => NamespaceQuota.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ClusterResourceQuotaMetadata>): ClusterResourceQuotaMetadata {
    return ClusterResourceQuotaMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClusterResourceQuotaMetadata>): ClusterResourceQuotaMetadata {
    const message = createBaseClusterResourceQuotaMetadata();
    message.name = object.name ?? "";
    message.uid = object.uid ?? "";
    message.totalLimits = object.totalLimits?.map((e) => QuotaResource.fromPartial(e)) || [];
    message.totalUsage = object.totalUsage?.map((e) => QuotaResource.fromPartial(e)) || [];
    message.quotas = object.quotas?.map((e) => NamespaceQuota.fromPartial(e)) || [];
    return message;
  },
};

function createBaseNamespaceQuota(): NamespaceQuota {
  return { namespace: "", limits: [], usage: [] };
}

export const NamespaceQuota: MessageFns<NamespaceQuota> = {
  encode(message: NamespaceQuota, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.namespace !== "") {
      writer.uint32(10).string(message.namespace);
    }
    for (const v of message.limits) {
      QuotaResource.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.usage) {
      QuotaResource.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NamespaceQuota {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNamespaceQuota();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.namespace = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.limits.push(QuotaResource.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.usage.push(QuotaResource.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NamespaceQuota {
    return {
      namespace: isSet(object.namespace) ? globalThis.String(object.namespace) : "",
      limits: globalThis.Array.isArray(object?.limits) ? object.limits.map((e: any) => QuotaResource.fromJSON(e)) : [],
      usage: globalThis.Array.isArray(object?.usage) ? object.usage.map((e: any) => QuotaResource.fromJSON(e)) : [],
    };
  },

  toJSON(message: NamespaceQuota): unknown {
    const obj: any = {};
    if (message.namespace !== "") {
      obj.namespace = message.namespace;
    }
    if (message.limits?.length) {
      obj.limits = message.limits.map((e) => QuotaResource.toJSON(e));
    }
    if (message.usage?.length) {
      obj.usage = message.usage.map((e) => QuotaResource.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<NamespaceQuota>): NamespaceQuota {
    return NamespaceQuota.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NamespaceQuota>): NamespaceQuota {
    const message = createBaseNamespaceQuota();
    message.namespace = object.namespace ?? "";
    message.limits = object.limits?.map((e) => QuotaResource.fromPartial(e)) || [];
    message.usage = object.usage?.map((e) => QuotaResource.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQuotaResource(): QuotaResource {
  return { resource: "", value: "0" };
}

export const QuotaResource: MessageFns<QuotaResource> = {
  encode(message: QuotaResource, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resource !== "") {
      writer.uint32(10).string(message.resource);
    }
    if (message.value !== "0") {
      writer.uint32(16).int64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuotaResource {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQuotaResource();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.resource = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QuotaResource {
    return {
      resource: isSet(object.resource) ? globalThis.String(object.resource) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "0",
    };
  },

  toJSON(message: QuotaResource): unknown {
    const obj: any = {};
    if (message.resource !== "") {
      obj.resource = message.resource;
    }
    if (message.value !== "0") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<QuotaResource>): QuotaResource {
    return QuotaResource.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QuotaResource>): QuotaResource {
    const message = createBaseQuotaResource();
    message.resource = object.resource ?? "";
    message.value = object.value ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
